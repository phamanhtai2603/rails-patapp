<li>
  <%= link_to gravatar_for user, size: 50  %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t("users.delete"), user, method: :delete, data: { confirm: t("users.delete_2") } %>
  <% end %>
</li>
